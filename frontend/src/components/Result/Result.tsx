import { Container } from "@nacteam/sdfui";

export const Result: React.FC = () => {
    return (
        <Container
            variant='secondary'
            style={{
                width: "calc(100% - 16px)",
            }}
        >
            Result
        </Container>
    );
}